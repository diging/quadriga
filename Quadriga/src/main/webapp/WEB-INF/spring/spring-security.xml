<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">
	<http pattern="/rest/**" auto-config="true" use-expressions="true"
		create-session="stateless">
		<intercept-url pattern="/rest/**"
			access="hasAnyRole('ROLE_QUADRIGA_USER_ADMIN','ROLE_QUADRIGA_USER_STANDARD','ROLE_QUADRIGA_USER_COLLABORATOR')" />
		<http-basic />
		<csrf disabled="true" />
	</http>

	<global-method-security pre-post-annotations="enabled" />

	<http auto-config="true" use-expressions="true"
		disable-url-rewriting="true">
		<form-login login-page="/login" default-target-url="/auth/welcome"
			authentication-failure-url="/loginfailed"
			authentication-success-handler-ref="authSuccessHandler" />
		<logout logout-success-url="/login" invalidate-session="true" /> 
	</http>

	<beans:bean id="encoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	<beans:bean id="authProvider"
		class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
		<beans:property name="userDetailsService" ref="userService" />
		<beans:property name="passwordEncoder" ref="encoder" />
	</beans:bean>
   <authentication-manager alias="authenticationManager"
		erase-credentials="false">
		 <authentication-provider>
			<user-service>
				<user name="${quadriga.admin.username}" password="${quadriga.admin.password}"
					authorities="ROLE_QUADRIGA_USER_ADMIN" />
			</user-service>
		</authentication-provider>
		<authentication-provider ref="authProvider"></authentication-provider>
	</authentication-manager>

</beans:beans>
